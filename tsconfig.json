{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": [
      // 编译时用的库
      "ESNext",
      "DOM",
      "DOM.Iterable"
    ]
  },
  "exclude": [
    // 排除掉哪些类库
    "node_modules",
    "**/__tests__"
  ]
}
